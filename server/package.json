{
  "name": "trackify-server",
  "version": "0.1.0",
  "private": true,
  "description": "Trackify backend — AI proxy + server features. Deploys free on Render or Heroku.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
